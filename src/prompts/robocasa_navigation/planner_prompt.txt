import numpy as np
from perception_utils import parse_query_obj
from action_utils import composer
# NOTE — runtime substitution:
#   The 'objects' lists below use placeholder names: 'goal' for the
#   destination and 'object_1', 'object_2', ... for non-goal items.
#   At runtime the harness replaces these placeholders with the actual
#   scene names. In your composer() string, refer to the goal as
#   'the goal' and to each non-goal item by its placeholder name
#   (object_1, object_2, ...) — do not substitute a concrete name
#   yourself.
#
# NOTE — value ranges:
#   distance: minimum 0, maximum 80 cm.
#   velocity: minimum 0, maximum 2 (1 = nominal speed). Express it as a
#   percent in the get_velocity_map query, where 100% = nominal.
#
# NOTE — what to emit:
#   Reference each object by its placeholder name and assign a specific
#   constraint per object (e.g. "keep at least 50cm from object_1, and
#   slow down to 40% velocity when within 30cm of object_2").
#   Name each one individually so the composer can ground it to a real scene object.
#
# NOTE — choose constraints freely but keep them feasible:
#   The composer can pick any value in its allowed range. Use the full
#   vocabulary — from "passing right next to" through "being careful
#   around" to "staying as far as possible". Avoid demanding so much
#   clearance that no feasible path remains for the robot.
#
# NOTE — only mention what the task needs:
#   If there is no obstacle, do not mention avoidance. If nominal speed
#   is fine, do not mention velocity. Combine all constraints for one
#   goal into a single composer() call. Use multiple composer() calls
#   only for multi-stage tasks.

objects = ['goal']
# Query: navigate safely to the goal while avoiding obstacles.
composer("move to the goal")
# done

objects = ['goal', 'object_1']
# Query: navigate safely to the goal while avoiding obstacles.
composer("move to the goal while keeping at least 20cm from object_1")
# done

objects = ['goal', 'object_1', 'object_2']
# Query: navigate safely to the goal while avoiding obstacles.
composer("move to the goal while keeping at least 70cm from object_1 and at least 30cm from object_2, slowing down to 50% velocity when within 30cm of object_1 or object_2")
# done

objects = ['goal', 'object_1', 'object_2', 'object_3']
# Query: navigate safely to the goal while avoiding obstacles.
composer("move to the goal while keeping at least 40cm from object_1, at least 30cm from object_2, and at least 25cm from object_3, slowing down to 30% velocity when within 30cm of any of them")
# done

objects = ['goal', 'object_1', 'object_2']
# Query: navigate safely to the goal while avoiding obstacles.
composer("move to the goal while keeping at least 35cm from object_1 and slowing down to 30% velocity when within 30cm of object_1, and keeping at least 60cm from object_2")
# done
